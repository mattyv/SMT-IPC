{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.c|*.cc|*.cpp|*.h|*.hpp) clang-format -i \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "clang-format"
          }
        ]
      }
    ]
  }
}
