# Compiled benchmark binaries
/smt_pingpong
/sibling_noise

# CMake build trees
/build*/

# Per-collaborator local Claude Code permission overrides (already covered
# by this user's global ignore, but not everyone's).
/.claude/settings.local.json

# Python bytecode cache
__pycache__/
*.pyc
